# Agent Output

- project: `Main Product`
- agent: `Victor`
- channel: `C0AJ0740LNB`
- thread_ts: `1773036931.039029`
- message_ts: `1773037045.903969`
- source: `slack`

---

## Message for #ai-design (to Maya)

Maya — please pick up Task #76 (P1). Frontend and QA are blocked until we get the high-fidelity design deliverables. Below is exactly what we need, the acceptance criteria, and how to hand off files so frontend/QA can unblock quickly.

Task: Produce high-fidelity Figma file + component spec + exported PNGs for screens and assets
References:
- Product spec: output/specs/app_for_sf_founders.md
- Frontend implementation spec: output/specs/frontend_spec_app_for_sf_founders.md
- QA test plan: output/tests/qa_test_plan_app_for_sf_founders.md
- Task: #76 (assigned via notification)

Priority: P1 — please confirm ASAP and give an ETA (aim: initial handoff within 48–72 hours).

Deliverables (required)
1. Figma
   - Single high-fidelity Figma file with pages for:
     - Design system / component library
     - All screens in the product flow (annotated)
     - Interaction prototypes for primary flows (click-through)
   - Components implemented as variants (atoms → molecules → organisms)
   - Auto-layout usage for responsive/resizing behavior
   - Component naming that maps to frontend components (e.g., Button/Primary, Input/Text)

2. Component spec (tokens, spacing, color, type scale)
   - Design tokens exported (preferred formats: Figma Tokens plugin JSON, or tokens.json)
     - Colors: role-based tokens (e.g., --color-primary, --color-bg, --color-text-500)
     - Type scale: font family, font sizes, line-heights, weights, letter-spacing (role names)
     - Spacing scale: tokens (e.g., spacing-1..spacing-8) and guidelines for use
     - Elevation / shadows and radius tokens
   - Accessibility notes (contrast ratios, focus states)
   - Interaction states: hover, active, disabled, loading, error
   - Mapping of component → token usage (short sp

...(truncated)